# Google Tag Manager
# El container ID es público (visible en el navegador), no es un secreto.
#
# Local:  copiá este archivo a .env y poné tu ID real, luego `npm run build`.
# Vercel: Project Settings → Environment Variables → GTM_ID
#
# Formato: GTM-XXXXXXX
GTM_ID=GTM-XXXXXXX
